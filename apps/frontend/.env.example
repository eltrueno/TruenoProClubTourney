# URL publica del backend (Docker en tu servidor)
PUBLIC_API_URL=https://api-torneo.midominio.com

# URL publica de la API de Better Auth en la raiz (para leer sesion en el navegador)
PUBLIC_ROOT_AUTH_URL=https://midominio.com/api/auth
